{
  "_what_this_is": [
    "THE STACK-SPECIFIC HALF OF THE HARNESS, in one file.",
    "",
    "`ci.py` is the step ladder: run each rung in order, print a positive marker with",
    "a count, name the rung that stopped the run. That part IS the same in every",
    "factory. What is NOT the same is every command it runs, so the commands live",
    "here instead of in the ladder.",
    "",
    "Set what you have. Leave a step empty and it is SKIPPED LOUDLY -- the log says",
    "<STEP>_SKIPPED and the gate can be told to require the marker, so a skipped rung",
    "is never mistaken for a passed one."
  ],
  "_examples": {
    "python": {
      "static": "python -m compileall -q .",
      "unit": "python -m pytest -q"
    },
    "python-uv": {
      "static": "uv run ruff check .",
      "unit": "uv run pytest -q"
    },
    "node": {
      "static": "npx tsc --noEmit",
      "unit": "npm test --silent"
    },
    "bun": {
      "static": "bun x tsc --noEmit",
      "unit": "bun test"
    },
    "go": {
      "static": "go vet ./...",
      "unit": "go test ./..."
    },
    "rust": {
      "static": "cargo clippy -- -D warnings",
      "unit": "cargo test"
    },
    "ruby": {
      "static": "bundle exec rubocop",
      "unit": "bundle exec rspec"
    },
    "dotnet": {
      "static": "dotnet build --nologo",
      "unit": "dotnet test --nologo"
    }
  },
  "static": "npx tsc --noEmit",
  "unit": "npm test --silent",
  "_unit_count_pattern": "A regex with ONE group capturing how many tests ran. ZERO IS NOT A PASS: a suite that discovered nothing exits 0 and looks perfect. Leave empty to skip the count check, and accept that you cannot tell a passing suite from an absent one.",
  "unit_count_pattern": "(\\d+) passed",
  "_driver": [
    "How the E2E reaches your software. THREE SHAPES cover almost everything:",
    "",
    "  http     a server. Started on a dynamic port, polled until healthy.",
    "  cli      a command. Invoked with args, stdout/stderr asserted.",
    "  library  no process at all. The E2E imports and calls it.",
    "",
    "A frontend app is `http`, and the agent driving the journeys uses a browser tool. A",
    "rendered window or a game loop is NONE of these -- the logic has to live behind",
    "a headless surface an E2E can drive, and that is an architecture decision to",
    "make before the code exists, not after."
  ],
  "_agent": [
    "WHO DRIVES THE JOURNEYS. `harness/END-TO-END.md` and",
    "`.factory/holdout/HOLDOUT.md` are markdown, and this command reads them, uses",
    "the running app, and writes a result file the gate checks.",
    "",
    "The prompt arrives on STDIN. Any coding-agent CLI with a headless mode works:",
    "  claude -p --allowedTools Bash,Read,Write --permission-mode acceptEdits",
    "  codex exec -",
    "  pi --mode json --print",
    "",
    "Whatever you put here must be able to RUN A SHELL COMMAND. An agent that can only",
    "edit files cannot use your app, and it will correctly report that it checked",
    "nothing.",
    "",
    "Empty is a FAILURE, not a skip. A gate that quietly drops its end-to-end rung",
    "reports green having never touched the app.",
    "",
    "Cost: this runs once per gate, plus once per planted defect during a mutation",
    "run. Point it at a cheaper model than the one writing the code -- the job is",
    "using an app and writing down what happened."
  ],
  "agent": {
    "cmd": "claude -p --allowedTools Bash,Read,Write --permission-mode acceptEdits",
    "timeout_s": 900
  },
  "driver": "http",
  "e2e_timeout_s": 300,
  "http": {
    "start": "npm run dev -- -p {port}",
    "health_path": "/api/health",
    "_health_contains": "A SUBSTRING OF THE RAW BODY, matched literally. The shipped default used to be {\"status\":\"ok\"} with no space, which does not appear in what json.dumps actually writes -- so the example app never came up, and the driver reported 'never became healthy' while printing a 200 and a healthy body. Keep it short and quote it the way YOUR app serialises. Empty means 200 alone is enough.",
    "health_contains": "ok",
    "boot_timeout_s": 45,
    "_env": "Optional extra environment for the app process. {port} is substituted.",
    "env": {}
  },
  "cli": {
    "_note": "The command under test. {args} is substituted per invocation.",
    "invoke": "python -m app.cli {args}",
    "smoke_args": "--version",
    "smoke_contains": ""
  },
  "library": {
    "_note": "Nothing to start. APP_STARTED is emitted after this import succeeds, which is the equivalent proof that the thing loads at all.",
    "import_check": "python -c \"import app\""
  },
  "browser": {
    "_note": "ONLY for an app whose correctness is visual. Leave install_check empty if no journey needs a browser. When it is set, `factory doctor` runs it at setup -- so a missing browser is a setup failure with an install command, not a mystery hang at gate time on a fresh runner.",
    "install_check": "",
    "install_hint": "npm i -g agent-browser && agent-browser install   |   pip install playwright && playwright install chromium"
  }
}
