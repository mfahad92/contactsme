name: factory-implement
description: |
  Take ONE accepted issue from the queue to an open pull request: prime, plan,
  implement, review, open. This is the PIV loop with the approvals removed.

  Use when: the dispatcher finds an issue labelled factory:accepted. By hand:
    archon workflow run factory-implement --branch factory/impl-12 "implement gh:issue:12"
  NOT for: triage (factory-triage), validating the PR this opens
  (factory-validate), or fixing one (factory-fix).

  IT DOES NOT MERGE AND IT DOES NOT JUDGE ITS OWN WORK. The last thing it does is
  open a PR labelled for the independent validator. A builder that can approve its
  own output is not being checked by anything.

  EVERY NODE GETS A FRESH SESSION. Not a performance choice: a node that inherits
  the previous node's context inherits its reasoning, and the whole independence
  argument rests on some nodes not having seen it.

  THE PREMIUM MODEL SITS IN THE PLAN SLOT. Two slots decide quality -- the one that
  plans and the one that implements -- and a premium model in ONE of them buys most
  of the quality of both. Premium in zero slots is what actually costs you.

model: medium

inputs:
  target:
    default: ""
    description: >-
      The issue to build, as gh:issue:<n>. When empty, it is parsed out of the
      run's trigger message.

returns: open-pr

nodes:
  - id: resolve
    script: resolve-target
    runtime: uv
    with:
      declared: "$INPUTS.target"
      kind: issue
    output_format:
      type: object
      properties:
        target: { type: string }
        number: { type: string }
      required: [target, number]

  # Everything that must be true before a single token is spent, and one thing that
  # must be true before anything can COMMIT: that no credential-shaped path is
  # unignored. An empty `git check-ignore` result means the commit step publishes
  # your key, and on a public repo that is publication, not a mistake you can take
  # back.
  - id: preflight
    script: preflight
    runtime: uv
    depends_on: [resolve]
    with:
      target: "$resolve.output.target"
    output_format:
      type: object
      properties:
        target: { type: string }
        number: { type: string }
        branch: { type: string }
        title: { type: string }
        base: { type: string }
      required: [target, number, branch, title, base]

  # --- 1. PRIME -------------------------------------------------------------
  # The plan node is the expensive one; feeding it a cold read of the repository is
  # how a premium model gets spent re-deriving what a cheap one could have handed
  # it. Read-only by construction: no Edit, no Write to source, no shell that
  # mutates.
  - id: prime
    command: prime
    model: small
    context: fresh
    depends_on: [preflight]
    # PROVEN BY PROBE, in both directions, before being believed:
    #   allowed_tools: ["Bash(python:*)"]   grants NOTHING -- the node has NO shell.
    #   allowed_tools: [Bash]               grants a shell.
    #   denied_tools:  ["Bash(git:*)"]      genuinely blocks it.
    # So the grant is coarse and the leash is the DENY. The first version scoped the
    # grant instead, and the build nodes ran for weeks with no shell at all: they
    # could not run the quick gate, said so politely in prose, and exited 0.
    allowed_tools: [Read, Glob, Grep, Write, Bash]
    denied_tools: ["Read(.factory/holdout/**)", "Glob(.factory/holdout/**)", "Grep(.factory/holdout/**)"]

  # --- 2. PLAN --------------------------------------------------------------
  # THE PREMIUM SLOT, and the last node that will ever go fully unattended -- this
  # is the step a human actually reads today, and everything downstream inherits
  # whatever it gets wrong.
  #
  # NO SHELL AT ALL. A plan node that can run things plans to measure something
  # itself, gets refused, and the refusal is silent: the request goes to a human who
  # is not there. Stating the measurement as the implement node's first task is the
  # working version of the same intent.
  - id: plan
    command: plan
    model: large
    context: fresh
    depends_on: [prime]
    allowed_tools: [Read, Glob, Grep, Write]
    denied_tools: ["Read(.factory/holdout/**)", "Glob(.factory/holdout/**)", "Grep(.factory/holdout/**)"]

  # An explicit escalation file beats a silent no-op. The plan node stops only for
  # the short list in commands/plan.md; when it does, this is the only thing that
  # turns that into a state change, and it CANCELS the run rather than letting five
  # more nodes run against a plan that says "do not build this".
  - id: gate-plan
    script: gate-plan
    runtime: uv
    depends_on: [plan]
    with:
      target: "$preflight.output.target"
    output_format:
      type: object
      properties:
        proceed: { type: boolean }
        reason: { type: string }
      required: [proceed, reason]

  - id: stop-escalated
    cancel: "The plan node escalated. The issue is parked at needs-human with the reason on it."
    depends_on: [gate-plan]
    when: "$gate-plan.output.proceed == false"

  # --- 3. IMPLEMENT ---------------------------------------------------------
  # Work the plan task by task, running each task's validation command as you go.
  # `Bash` is scoped to the QUICK gate and to python one-liners for measurement --
  # the leash was never what enforced the independence property anyway. What stops a
  # builder tuning against the gate is that the VALIDATOR re-runs everything in a
  # separate process, that every deliberate defect must still be caught, and that
  # the ratchet refuses a quieter harness.
  - id: implement
    command: implement
    model: medium
    context: fresh
    depends_on: [gate-plan]
    when: "$gate-plan.output.proceed == true"
    allowed_tools: [Read, Glob, Grep, Edit, Write, Bash]
    denied_tools: ["Read(.factory/holdout/**)", "Glob(.factory/holdout/**)", "Grep(.factory/holdout/**)", "Bash(git:*)", "Bash(gh:*)"]
    idle_timeout: 900000

  # --- 4. COMMIT ------------------------------------------------------------
  # WITHOUT THIS THE WHOLE LAP IS THEATRE. The implement node edits files in the
  # worktree, nothing records them, and the worktree is discarded: every node
  # reports OK, the guard correctly sees two changed files, and the branch ends up
  # empty. Driving the lap by hand hides it, because a human commits without being
  # told to.
  #
  # It also ASSERTS ON THE ARTIFACT rather than the exit code. A node that was
  # denied a tool exits 0 having changed nothing, and "the run succeeded" is then
  # true and useless.
  - id: commit
    script: commit
    runtime: uv
    depends_on: [implement]
    with:
      number: "$preflight.output.number"
      title: "$preflight.output.title"
      base: "$preflight.output.base"
    output_format:
      type: object
      properties:
        sha: { type: string }
        files: { type: integer }
      required: [sha, files]

  # --- 5. GUARD -------------------------------------------------------------
  # Protected paths and the scope caps, BEFORE anything else is evaluated: a change
  # that can edit the rulebook must not be judged against the edited rulebook. Fails
  # CLOSED -- a diff that cannot be computed is not a diff that was checked.
  - id: guard
    script: guard-branch
    runtime: uv
    depends_on: [commit]
    with:
      base: "$preflight.output.base"
      target: "$preflight.output.target"

  # --- 6. SELF-CHECK --------------------------------------------------------
  # The full gate, run here for the BUILDER's benefit. Nothing downstream trusts it
  # -- the validator re-runs everything in its own process -- but a lap that opens a
  # PR it already knows is red wastes a whole validation cycle to learn it.
  - id: selfcheck
    script: selfcheck
    runtime: uv
    depends_on: [guard]
    timeout: 1800000

  # --- 7. REVIEW ------------------------------------------------------------
  # The only node that reads the diff AS CODE rather than as a set of markers. It
  # writes the PR record; it does not open the PR. A model's only output is a
  # record, and code decides what happens to it.
  - id: review
    command: review
    model: medium
    context: fresh
    depends_on: [selfcheck]
    allowed_tools: [Read, Glob, Grep, Write, Bash]
    denied_tools: ["Read(.factory/holdout/**)", "Glob(.factory/holdout/**)", "Grep(.factory/holdout/**)", "Bash(gh:*)", "Bash(git push:*)", "Bash(git commit:*)"]

  # --- 8. OPEN THE PR -------------------------------------------------------
  # Push and open, by the script, from the review node's file. Same rule as the
  # merge: a node holding `gh pr create` is a node that can open a PR against any
  # branch it likes, including one nothing validated.
  - id: open-pr
    script: open-pr
    runtime: uv
    depends_on: [review]
    with:
      target: "$preflight.output.target"
      number: "$preflight.output.number"
      branch: "$preflight.output.branch"
      base: "$preflight.output.base"
    output_format:
      type: object
      properties:
        pr: { type: string }
        url: { type: string }
      required: [pr, url]
